#!/bin/bash
# ==============================================================================
# ПРИМЕР ФАЙЛА ЛОКАЛЬНЫХ НАСТРОЕК (TEMPLATE)
# ==============================================================================
#
# Как использовать:
# 1. Скопируйте этот файл: cp config_local.example config_local.sh
# 2. Отредактируйте: nano config_local.sh
# 3. Впишите свои токены и измените нужные пороги.
#
# Файл config_local.sh НИКОГДА не перезаписывается при обновлении скриптов.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. НАСТРОЙКИ TELEGRAM (ОБЯЗАТЕЛЬНО)
# ------------------------------------------------------------------------------
# Получить токен у @BotFather, ID у @userinfobot
BOT_TOKEN="123456789:AAHg..."
CHAT_ID="-200223..."

# ------------------------------------------------------------------------------
# 2. ОБСЛУЖИВАНИЕ И ТИШИНА (MAINTENANCE WINDOWS)
# ------------------------------------------------------------------------------
# Формат времени: только часы (0-23).

# ГЛОБАЛЬНОЕ ОКНО: В это время ВСЕ уведомления заглушены.
# Полезно для плановых перезагрузок или работ в ЦОД.
# Пример: с 02:00 до 04:00
# GLOBAL_MNT_START=2
# GLOBAL_MNT_END=4

# ЛОКАЛЬНОЕ ОКНО (ТОЛЬКО IO WAIT): В это время игнорируется нагрузка на диск.
# Полезно для ночных бэкапов.
# Пример: с 23:00 до 08:00
# IOWAIT_MNT_START=23
# IOWAIT_MNT_END=8

# ПЕРИОД ПОВТОРА УВЕДОМЛЕНИЙ (в секундах)
# Как часто напоминать о висящей проблеме. 3600 сек = 1 час.
# ALERT_MUTE_PERIOD=3600

# ------------------------------------------------------------------------------
# 3. ПОРОГИ СРАБАТЫВАНИЯ (THRESHOLDS)
# ------------------------------------------------------------------------------
# Раскомментируйте переменную, чтобы изменить её значение по умолчанию.

# CPU: Тревога, если Load Average превышает (Кол-во ядер * Порог / 100)
# CPU_THRESHOLD=80

# RAM: Тревога, если свободной памяти меньше X%
# RAM_THRESHOLD=10

# DISK: Тревога, если свободного места меньше X%
# DISK_THRESHOLD=10

# IO WAIT: Тревога, если ожидание диска больше X% (тормоза дисков)
# IOWAIT_THRESHOLD=10.0

# SWAP: Тревога, если занято больше X% свопа
# SWAP_THRESHOLD=60

# TEMP: Температура процессора/системы (градусы Цельсия)
# TEMP_WARNING=80    # Предупреждение
# TEMP_CRITICAL=95   # Критично

# UPTIME: Тревога, если сервер работает меньше X минут (значит был ребут)
# UPTIME_MIN_MINUTES=60

# ------------------------------------------------------------------------------
# 4. СПЕЦИФИЧНЫЕ НАСТРОЙКИ
# ------------------------------------------------------------------------------

# ДИСКИ: Исключения из проверки места
# Типы файловых систем (через |)
# DISK_EXCLUDE_TYPE="tmpfs|devtmpfs|squashfs|overlay"
# Пути монтирования (через |)
# DISK_EXCLUDE_PATH="/snap|/run|/var/lib/docker"

# SMART: Лимиты ошибок дисков
# SMART_REALLOCATED_LIMIT=5
# SMART_PENDING_LIMIT=0

# QEMU AGENT: Фильтр имен виртуальных машин
# Оставьте пустым, чтобы проверять все. Впишите "win", чтобы искать только *win*.
# VM_NAME_FILTER=""
