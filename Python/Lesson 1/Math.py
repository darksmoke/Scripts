# - — вычитание
# * — умножение
# ** — возведение в степень
# / — деление
# // — целочисленное деление
# % — остаток от деления

source = 'The Twins'
destination = 'The Eyrie'

# BEGIN (write your solution here)
t = "'{}-{}'"
res2 = t.format(source, destination)
print(res2)


# Существуют сокращённые формы для всех арифметических операций и для конкатенации строк:

a = a + 1 → a += 1
a = a - 1 → a -= 1
a = a * 2 → a *= 2
a = a / 1 → a /= 1